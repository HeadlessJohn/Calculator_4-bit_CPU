ACC : accumulator 누산기

PC : program counter
명령 실행할때마다 1씩 증가
for문-> 반복할 특정 번지로 이동
if문-> 분기한 특정 번지로 이동
(loadable counter와 유사함)

ROM :binary 코드를 넣을 메모리


클럭이 들어가면 pc에서 주소 0을 내보냄

MAR에서 PC를 받아서 ROM의 0번지의 코드를 MDR에 내보냄

IR (instruction register)에 MDR의 코드를 저장
control block에서 IR을 해석해서 명령어를 실행